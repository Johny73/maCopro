{% form_theme form 'proprio/formThemes/themeProprio.html.twig' %}

{{ form_start(form) }}
{{ form_widget(form) }}
<input type="submit" id="{{ actionValue }}" name="{{ actionValue }}" value="{{ action }}" />
<input  type="submit" id="{{ action2Value }}" name="{{ action2Value }}" value="{{ action2 }}" />
{{ form_end(form) }}