{% extends 'base.html.twig' %}

{% block title %}Informations{% endblock %}

{% block body %}

    {% for article in articles %}
        <h2><a href="{{ path('infos_article', {'id': article.id}) }}">{{ article.titre }}</a></h2>

    {% endfor %}
    {{ knp_pagination_render(articles) }}
{% endblock %}
